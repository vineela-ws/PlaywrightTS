import { test, expect } from '@playwright/test'

test('Playwright TypeScript Test1', async ({ page }) => {

})

test('Playwright TypeScript Test2', async ({ page }) => {
  expect(true).toBe(false);
})

test('Playwright TypeScript Test3', async ({ page }) => {
  expect(true).toBe(false);
})


